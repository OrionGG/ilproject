package Classificator;

import java.util.HashMap;

import DBLayer.DAOCategorization;



public class Evaluator {


public static int main(HashMap urlsCategorizadas){
	double score=0;
	double totalScore=0;
	while(urlsCategorizadas.get(key)){
		for(urls){
			score=FinalScoreCalculator.getScoreWeb(url,cat);
			totalScore=score+totalScore;
		}
		
	}
	System.out.print("Total score of evaluating "+totalScore);
	
	return 0;
	
	
}
}
